export async function validateProductData() {
  // TODO: Get all products from Shopify
  // if possible only get the products that fits within the paramaters
  // PARAMATERS:
  // - Taxable!: false (should ALWAYS be true!!!)
  // - HC Code!: empty (Should 99% of the time be 1806.90)
  // - Barcode: empty (Should ALWAYS be filled)
  // - SKU: empty (Should ALWAYS be filled)
  // - Weight: empty (Should ALWAYS be filled)
}
